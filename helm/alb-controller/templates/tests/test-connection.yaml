apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "alb-controller.fullname" . }}-test-connection"
  labels:
    {{- include "alb-controller.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "alb-controller.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
