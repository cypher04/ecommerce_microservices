apiVersion: v1
kind: Service
metadata:
  name: {{ include "alb-controller.fullname" . }}
  labels:
    {{- include "alb-controller.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "alb-controller.selectorLabels" . | nindent 4 }}
