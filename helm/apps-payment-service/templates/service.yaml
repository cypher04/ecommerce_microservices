apiVersion: v1
kind: Service
metadata:
  name: {{ include "apps-payment-service.fullname" . }}
  labels:
    {{- include "apps-payment-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "apps-payment-service.selectorLabels" . | nindent 4 }}
