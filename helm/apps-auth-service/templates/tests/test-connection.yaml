apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "apps-auth-service.fullname" . }}-test-connection"
  labels:
    {{- include "apps-auth-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "apps-auth-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
